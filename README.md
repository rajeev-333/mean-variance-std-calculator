# Mean-Variance-Standard Deviation Calculator

A Python project built as part of the **FreeCodeCamp Data Analysis with Python** certification.

## Description
A `calculate()` function that accepts a list of 9 numbers, converts it into a 3×3 NumPy matrix, and returns the mean, variance, standard deviation, max, min, and sum along both axes and for the flattened matrix.

## How to Run
```bash
python main.py
